always first read the full README.md, it is written to be maximally useful for both humans and ai coding agents

Documented solutions live in `docs/solutions/`: past bugs, workflow issues, conventions, and best practices with YAML frontmatter (`module`, `tags`, `problem_type`). Relevant when implementing, debugging, or changing documented areas.
